## 
vben admin官方文档：https://doc.vben.pro/guide/essentials/build.html

ruoyi-plus-vben5文档：https://dapdap.top/
## 启动项目

没有修改端口默认为5666，确保后端服务已经启动

启动：`pnpm run dev:antd`
打包：`pnpm build:antd`

全局搜索：快速开始 {#quick-start}

账号密码：admin/admin123

## 开发目录

具体开发路径为根目录/apps/web-antd, 项目开发只需要关注这个目录

## 菜单图标导入

使用iconify图标 可导入根目录/scripts/菜单图标替换sql/update_icon.sql进行替换

## 打包项目

打包后的目录：/apps/web-antd/dist 目录下生成生产环境代码 `pnpm build:antd`

nginx配置

```shell
upstream ruoyi_server {
        server 127.0.0.1:8080;
        #server 127.0.0.1:9003 backup;  #热备
}

upstream ruoyi_monitor {
        server 127.0.0.1:9090;
}

upstream snailjob_server {
        server 127.0.0.1:8800;
}

server  {
        listen       80;
        server_name  localhost;

        # 默认跳转VuePress
        location / {
                root /data/nginx_html/vuePress/dist;
                # root /var/www/html;
                try_files $uri $uri/ /index.html;
                index index.html index.htm;
        }

        # 跳转若依后台
        location /ry {
                # 不缓存html，防止程序更新后缓存继续生效
                if ($request_filename ~* .*\.(?:htm|html)$) {
                        add_header Cache-Control "private, no-store, no-cache, must-revalidate, proxy-revalidate";
                        access_log on;                     
                }
                # 这里是vue打包文件dist内的文件的存放路径
                alias   /data/nginx_html/ruoyi/dist;
                # 这里要加/ry/,不然会刷新不了
                try_files $uri $uri/ /ry/index.html; 
                index index.html index.htm;
        }

        # 跳转spring boot admin
        # https 会拦截内链所有的 http 请求 造成功能无法使用
        # 解决方案1 将 admin 服务 也配置成 https
        # 解决方案2 将菜单配置为外链访问 走独立页面 http 访问
        location /admin/ {
                proxy_set_header Host $http_host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header REMOTE-HOST $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_pass http://ruoyi_monitor/admin/;
        }

        location /snail-job/ {
                proxy_set_header Host $http_host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header REMOTE-HOST $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_pass http://snailjob_server/snail-job/;
        }

        # 跳转nacos
        location /nacos {
                proxy_pass  http://127.0.0.1:10100;
        }

        # 限制外网访问内网 actuator 相关路径
        location ~ ^(/[^/]*)?/actuator(/.*)?$ {
            return 403;
        }
        
        # 跳转后端接口
        location /prod-api/ {
                proxy_set_header Host $http_host;
                proxy_set_header X-Real-IP $remote_addr;
                proxy_set_header REMOTE-HOST $remote_addr;
                proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
                proxy_read_timeout 86400s;
                # sse 与 websocket参数
                proxy_http_version 1.1;
                proxy_set_header Upgrade $http_upgrade;
                proxy_set_header Connection "upgrade";
                proxy_buffering off;
                proxy_cache off;
                proxy_pass http://ruoyi_server/;
        }

        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }
}


```

## 1. 第一次启动

官方文档：https://dapdap.top/guide/quick-start.html 必须使用node>20+ & pnpm安装依赖

### 1.1. 全局安装PNPM

npm install --global pnpm

### 1.2. 查看版本

pnpm -v

### 1.3. 设置下载源

pnpm config get registry

### 1.4. 切换源：

使用以下命令切换到淘宝npm镜像源：pnpm config set registry https://registry.npmmirror.com/

### 1.5. 如果你需要还原到默认的npm源，可以使用：

pnpm config set registry https://registry.npmjs.org

### 1.6. 查看存储路径

默认是C:\Users\10938\AppData\Local\pnpm\store\v3 pnpm store path

### 1.7. 修改存储路径

（官网：pnpm config set store-dir /path/to/.pnpm-store）pnpm config set store-dir D:/pnpm/.pnpm-store

## 生产部署

查看状态systemctl status nginx

nginx -s reload

nginx -t 测试配置文件是否正确
